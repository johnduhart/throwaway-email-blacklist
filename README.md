This repo contains a file (domains.txt) which lists domains that spend mail as a throwaway service.

The file is formatted as one domain per line, with lines starting with an hash symbol (#) are comments.
